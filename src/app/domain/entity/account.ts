export class Register {
    email:String;
    name:String;
    password:String;
    phoneNumber:String;
    receiveConsent:boolean;
    terms:Array<any>;
}

export class Account {
  cbusinessLicenseFileName: string;
  cbusinessLicenseFilePath: string;
  cbusinessLicenseNumber: string;
  cceo: string;
  ccompanyName: string;
  email: string;
  name: string;
  pbusinessLicenseFileName: string;
  pbusinessLicenseFilePath: string;
  pbusinessLicenseNumber: string;
  pceo: string;
  pcompanyName: string;
  phoneNumber: string;
  pbusinessLicenseFileSeq: string;
  cbusinessLicenseFileSeq: string;
  receiveConsent: boolean;
  emailReceiveConsent: boolean;
}
